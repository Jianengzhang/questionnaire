<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.questionnaire.ssm.module.userManage.mapper.UnitManageMapper">
    <insert id="insertUnit" parameterType="com.questionnaire.ssm.module.generated.pojo.Unit">

        <selectKey keyProperty="unitId" order="AFTER" resultType="java.lang.Long">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into unit (unit_name, unit_code, unit_level,
        unit_province, unit_city, address
        )
        values (#{unitName,jdbcType=VARCHAR}, #{unitCode,jdbcType=VARCHAR}, #{unitLevel,jdbcType=VARCHAR},
        #{unitProvince,jdbcType=VARCHAR}, #{unitCity,jdbcType=VARCHAR}, #{address,jdbcType=VARCHAR}
        )
    </insert>
</mapper>